#include <unordered_map>
#include <string>
#include <vector>

using namespace std;
template <typename T>
class Field {
    private:
    string name;
    vector<T> entries;
    public:
    string get_name() { return name; }
    void add_entry 
    
};