# bronzeDB
NoSQL C++ DB server.

## Prerequisites

- g++ with C++17 support

## Build

```bash
cd src/build
make
```

This compiles the `bronzedb` binary in `src/build/`.

To clean build artifacts:

```bash
make clean
```
